const ItemFriendPhone = (props) => {
  //   const friendName = "James";

  return (
    <li>
      <span>{props.name}</span>
      <span>{props.phone}</span>
    </li>
  );
};

export default ItemFriendPhone;
